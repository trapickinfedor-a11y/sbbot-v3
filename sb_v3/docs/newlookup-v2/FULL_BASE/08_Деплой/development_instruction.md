# Пошаговая инструкция по разработке Newlookup

Это чеклист задач для написания кода. Выполняй шаги последовательно. Каждый шаг — это конкретная задача, которую нужно сделать.

**Главное правило:** Перед каждой задачей в Cursor выделяй весь текст файла `newlookup_srs_v12_full.md` и добавляй его в чат, чтобы AI всегда был в контексте.

---

### Фаза 1: Настройка проекта и База Данных (Приоритет: P0 - Критический)

**Цель:** Подготовить скелет проекта, настроить конфигурацию и создать все модели базы данных.

-   [ ] **Задача 1: Создать файловую структуру**
    -   **Что сделать:** Создать все папки и пустые файлы, как описано в разделе 2 документа `cursor_strategy.md`.
    -   **Что написать:** Ничего, просто создать структуру.

-   [ ] **Задача 2: Написать файл конфигурации**
    -   **Что сделать:** Написать код, который будет читать все переменные из `.env` файла.
    -   **Куда писать:** `app/core/config.py`.
    -   **Что написать:** Код на Pydantic `BaseSettings`, который загружает все переменные, перечисленные в разделе 2.3 ТЗ.

-   [ ] **Задача 3: Написать модели базы данных**
    -   **Что сделать:** Описать все таблицы базы данных в виде классов SQLAlchemy.
    -   **Куда писать:** Разделить на 3 файла:
        1.  `app/db/models/user_related.py` (модели `User`, `Seller`, `Marketer`, `Admin`, `Worker`).
        2.  `app/db/models/product_related.py` (все модели `seller_*_items`, `brute_bank_*`, `esim_plans`).
        3.  `app/db/models/system.py` (`Transaction`, `WithdrawalRequest`, `Coupon`, `SupportTicket` и т.д.).
    -   **Что написать:** Классы SQLAlchemy 2.0 с аннотациями типов, строго по структуре из раздела 3 ТЗ.

-   [ ] **Задача 4: Настроить подключение к БД**
    -   **Что сделать:** Написать код для создания асинхронного подключения к PostgreSQL.
    -   **Куда писать:** `app/core/db.py`.
    -   **Что написать:** Функции `create_async_engine` и `async_sessionmaker`.

---

### Фаза 2: Ядро Клиентского Бота (Приоритет: P0-P1)

**Цель:** Запустить всех ботов и реализовать базовый функционал для конечного пользователя.

-   [ ] **Задача 5: Написать главный запускающий файл**
    -   **Что сделать:** Написать скрипт, который одновременно запускает всех 6 ботов.
    -   **Куда писать:** `main.py`.
    -   **Что написать:** Код, который импортирует и запускает `Dispatcher` для каждого бота через `asyncio.gather`.

-   [ ] **Задача 6: Реализовать регистрацию в Mirror Bot**
    -   **Что сделать:** Написать обработчик команды `/start`.
    -   **Куда писать:** `app/bots/mirror_bot/handlers.py`.
    -   **Что написать:** Логику принятия правил, создания пользователя в БД и обработки реферальных ссылок (раздел 4.1 ТЗ).

-   [ ] **Задача 7: Реализовать динамическое главное меню**
    -   **Что сделать:** Написать функцию, которая строит клавиатуру главного меню.
    -   **Куда писать:** `app/bots/mirror_bot/keyboards.py`.
    -   **Что написать:** Функция, которая берёт данные из таблицы `mirror_menu_categories` и генерирует `InlineKeyboardMarkup` (раздел 4.2 ТЗ).

---

### Фаза 3: Финансы - Купоны и Платежи (Приоритет: P1)

**Цель:** Реализовать пополнение баланса и систему скидок.

-   [ ] **Задача 8: Реализовать логику купонов**
    -   **Что сделать:** Написать FSM для ввода и применения купона.
    -   **Куда писать:** `app/bots/mirror_bot/handlers.py` и `app/db/repository.py`.
    -   **Что написать:** Код, который проверяет все условия купона (срок, лимиты, одноразовость) и создаёт запись в `coupon_redemptions` (раздел 4.3, REQ-MB-008).

-   [ ] **Задача 9: Интегрировать платёжные системы**
    -   **Что сделать:** Написать классы для работы с BTCPay Server и Helecat.
    -   **Куда писать:** `app/services/payments.py`.
    -   **Что написать:** Функции для создания счёта на оплату и проверки его статуса (раздел 4.3 ТЗ).

---

### Фаза 4: Функционал Продавца (Приоритет: P1)

**Цель:** Дать продавцам возможность вносить депозит и загружать товары.

-   [ ] **Задача 10: Реализовать внесение депозита**
    -   **Что сделать:** Написать флоу для оплаты залогового депозита $600.
    -   **Куда писать:** `app/bots/seller_bot/handlers.py`.
    -   **Что написать:** Код, который использует `app/services/payments.py` для приёма депозита и активации продавца (раздел 5.1 ТЗ).

-   [ ] **Задача 11: Реализовать FSM-конфигураторы для загрузки товаров**
    -   **Что сделать:** Написать пошаговые диалоги для загрузки **каждого** из 9 типов товаров.
    -   **Куда писать:** В папке `app/bots/seller_bot/` создать отдельные файлы для каждой категории: `handlers_selfreg_ba.py`, `handlers_cc.py`, `handlers_logs_ba.py` и т.д.
    -   **Что написать:** Код FSM (машины состояний) для каждой категории, который задаёт вопросы продавцу и собирает данные строго по спецификациям из раздела 13 ТЗ.

---

### Фаза 5: Функционал Поддержки и Модерации (Приоритет: P1-P2)

**Цель:** Дать команде поддержки инструменты для управления системой.

-   [ ] **Задача 12: Реализовать ролевую модель (RBAC)**
    -   **Что сделать:** Написать middleware для Aiogram.
    -   **Куда писать:** Создать новый файл `app/bots/support_bot/middleware.py`.
    -   **Что написать:** Middleware, которое перед выполнением команды в Support Bot проверяет роль сотрудника в таблице `admins` (раздел 8.1 ТЗ).

-   [ ] **Задача 13: Реализовать обработку вывода средств**
    -   **Что сделать:** Написать хендлеры для роли `finance`.
    -   **Куда писать:** `app/bots/support_bot/handlers.py`.
    -   **Что написать:** Код, который присылает уведомление о заявке на вывод, показывает кнопки "Одобрить"/"Отклонить" и обрабатывает их (раздел 8.2 ТЗ).

-   [ ] **Задача 14: Реализовать модерацию товаров**
    -   **Что сделать:** Написать хендлеры для роли `moderator`.
    -   **Куда писать:** `app/bots/support_bot/handlers.py`.
    -   **Что написать:** Код, который присылает уведомление о новом товаре на модерацию и позволяет его одобрить или отклонить (раздел 8.4 ТЗ).

---

### Фаза 6: Веб-интерфейсы (Приоритет: P2)

**Цель:** Создать API для Admin Panel и Seller Mini App.

-   [ ] **Задача 15: Настроить FastAPI и авторизацию**
    -   **Что сделать:** Подготовить основу для веб-сервера.
    -   **Куда писать:** `app/web/main.py`, `app/web/schemas.py`, `app/web/routers/admin_panel.py`.
    -   **Что написать:** Базовое приложение FastAPI, Pydantic-схемы для всех моделей БД и эндпоинт `/login` с JWT-авторизацией.

-   [ ] **Задача 16: Реализовать CRUD-эндпоинты**
    -   **Что сделать:** Написать API для управления меню и купонами.
    -   **Куда писать:** `app/web/routers/admin_panel.py`.
    -   **Что написать:** Эндпоинты для создания, чтения, обновления и удаления категорий меню (раздел 11.2 ТЗ) и купонов (раздел 11.4 ТЗ).

-   [ ] **Задача 17: Реализовать конфигуратор Brute Bank**
    -   **Что сделать:** Написать API для управления группами Brute Bank.
    -   **Куда писать:** `app/web/routers/admin_panel.py`.
    -   **Что написать:** Эндпоинты, которые позволяют создавать и редактировать группы Brute Bank в соответствии с конфигуратором из раздела 11.7 ТЗ.
