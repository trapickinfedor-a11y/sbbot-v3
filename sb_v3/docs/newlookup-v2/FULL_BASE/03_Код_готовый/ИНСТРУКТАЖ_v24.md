# Инструктаж: Обновление v2 → v24

**Статус проверки:** ✅ 3 прохода завершены, синтаксис проверен Python AST  
**Дата:** 14.03.2026  
**Совместимость:** Точно под архитектуру вашего v2 (aiogram + SQLAlchemy + PostgreSQL/SQLite)

---

## Что входит в этот пакет

| Файл | Что делает | Куда копировать |
|---|---|---|
| `shared/database/models_v24_additions.py` | Документация новых моделей и колонок | Читай, добавляй вручную в models.py |
| `alembic/versions/v24_001_additions.py` | Миграция БД | `alembic/versions/` |
| `mirror_bot/services/product_service_v24.py` | Исправленный сервис покупки | Замени `product_service.py` |
| `seller_bot/handlers/orders_v24.py` | Исправленный хендлер заказов | Замени `orders.py` |
| `shared/utils/chat_filter_v24.py` | Расширенный фильтр контактов | Замени `chat_filter.py` |
| `shared/tasks/auto_complete.py` | Celery задачи | Создай новый файл |

---

## Шаг 1 — Резервная копия

**Обязательно перед любыми изменениями:**

```bash
# Резервная копия кода
cp -r /path/to/newlookup_v2_work /path/to/newlookup_v2_BACKUP_$(date +%Y%m%d)

# Резервная копия БД (PostgreSQL)
pg_dump -U newlookup newlookup > backup_$(date +%Y%m%d).sql

# Резервная копия БД (SQLite)
cp data/newlookup.db data/newlookup_BACKUP_$(date +%Y%m%d).db
```

---

## Шаг 2 — Применить миграцию БД

```bash
cd /path/to/newlookup_v2_work

# Узнай ID последней миграции
alembic history

# Открой файл alembic/versions/v24_001_additions.py
# Замени строку:
#   down_revision = None
# на:
#   down_revision = 'ID_ПОСЛЕДНЕЙ_МИГРАЦИИ'
# (ID из вывода alembic history, например: 'abc123def456')

# Скопируй файл миграции
cp /path/to/v24_001_additions.py alembic/versions/

# Применяй миграцию
alembic upgrade head

# Проверь что всё применилось
alembic current
```

**Если alembic не настроен** (нет alembic.ini):
```bash
# Создай таблицы напрямую через psql
psql -U newlookup -d newlookup -f /path/to/migration_manual.sql
```

---

## Шаг 3 — Обновить shared/database/models.py

Открой `shared/database/models.py` и добавь новые колонки.  
Читай файл `models_v24_additions.py` — там все инструкции в комментариях.

**Критические изменения:**

### В класс `SellerOrder` добавь:
```python
# Идемпотентность финансовых операций
escrow_released: Mapped[bool] = mapped_column(Boolean, default=False)
marketer_commission_paid: Mapped[bool] = mapped_column(Boolean, default=False)
worker_paid: Mapped[bool] = mapped_column(Boolean, default=False)

# Таймеры
auto_complete_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
dispute_deadline_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)

# Маркетолог
marketer_id: Mapped[Optional[int]] = mapped_column(ForeignKey("marketers.id"), nullable=True)
marketer_commission_amount: Mapped[Optional[Decimal]] = mapped_column(Numeric(10, 2), nullable=True)
```

### В класс `Product` добавь:
```python
is_active: Mapped[bool] = mapped_column(Boolean, default=True)
moderation_status: Mapped[str] = mapped_column(String(30), default="pending_moderation")
moderation_comment: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
moderated_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
moderated_by: Mapped[Optional[int]] = mapped_column(BigInteger, nullable=True)
```

### В класс `Worker` добавь:
```python
violation_count: Mapped[int] = mapped_column(Integer, default=0)
is_suspended: Mapped[bool] = mapped_column(Boolean, default=False)
suspended_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
suspended_reason: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
```

### В класс `User` добавь (если нет):
```python
language: Mapped[str] = mapped_column(String(10), default='ru')
trust_score: Mapped[int] = mapped_column(Integer, default=100)
last_active_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
```

### Добавь новый класс `SystemSetting`:
```python
class SystemSetting(Base):
    __tablename__ = "system_settings"
    
    key: Mapped[str] = mapped_column(String(100), primary_key=True)
    value: Mapped[str] = mapped_column(Text, nullable=False)
    description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

---

## Шаг 4 — Заменить product_service.py

```bash
# Замени файл
cp product_service_v24.py mirror_bot/services/product_service.py
```

**Что изменилось:**
- `SELECT FOR UPDATE` — защита от одновременной покупки одного товара
- Проверка `product.is_active` и `product.moderation_status`
- Реферальный бонус начисляется сразу, комиссия маркетолога — после эскроу
- Ответ не содержит данных продавца

---

## Шаг 5 — Заменить seller_bot/handlers/orders.py

```bash
cp orders_v24.py seller_bot/handlers/orders.py
```

**Что изменилось:**
- **УБРАНА** строка `f"👤 Buyer ID: <code>{order.buyer_user_id}</code>"`  
  Продавец больше не видит Telegram ID покупателя
- Добавлен фильтр контента для текстов продавца
- Добавлен статус `disputed` в отображение
- Добавлен таймер автозавершения

---

## Шаг 6 — Заменить chat_filter.py

```bash
cp chat_filter_v24.py shared/utils/chat_filter.py
```

**Что изменилось:**
- Добавлена фильтрация email-адресов
- Добавлена фильтрация Discord/WhatsApp/Signal
- Новая функция `filter_and_log()` для воркеров — фильтрует и логирует нарушение в БД

**Обновить вызовы в worker_bot:**
```python
# БЫЛО:
filtered = filter_message(text)

# СТАЛО (в worker_bot handlers):
from shared.utils.chat_filter import filter_and_log
filtered, violated = await filter_and_log(
    text=message.text,
    worker_id=worker.id,
    order_id=order_id,
    session=session
)
if violated:
    await message.answer("⚠️ Contact information was removed from your message.")
```

---

## Шаг 7 — Добавить Celery (новый функционал)

### 7.1 Установить зависимости:
```bash
pip install celery[redis] kombu
```

### 7.2 Создать файл:
```bash
mkdir -p shared/tasks
touch shared/tasks/__init__.py
cp auto_complete.py shared/tasks/
```

### 7.3 Добавить в docker-compose.yml:
```yaml
  celery_worker:
    build:
      context: .
      dockerfile: main_bot/Dockerfile  # или общий Dockerfile
    container_name: newlookup_celery_worker
    command: celery -A shared.tasks.auto_complete.celery_app worker --loglevel=info -Q default
    env_file: .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./data:/app/data
    networks:
      - bot_network
    restart: unless-stopped

  celery_beat:
    build:
      context: .
      dockerfile: main_bot/Dockerfile
    container_name: newlookup_celery_beat
    command: celery -A shared.tasks.auto_complete.celery_app beat --loglevel=info
    env_file: .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
    depends_on:
      - celery_worker
    networks:
      - bot_network
    restart: unless-stopped
```

### 7.4 Убедиться что REDIS_URL заполнен в .env:
```bash
REDIS_URL=redis://redis:6379/0
```

---

## Шаг 8 — Установить auto_complete_at при завершении заказа

В `seller_bot/handlers/orders.py` при установке `order.status = "completed"` добавь:

```python
from datetime import timedelta

# Устанавливаем таймер автозавершения (24 часа)
order.auto_complete_at = datetime.utcnow() + timedelta(hours=24)
order.dispute_deadline_at = datetime.utcnow() + timedelta(hours=24)
```

---

## Шаг 9 — Проверить seller_bot/handlers/chat.py

Открой `seller_bot/handlers/chat.py` и убедись что:

1. Продавец не видит username/ID покупателя в чате
2. Все тексты продавца проходят через `filter_message()`

```python
# Добавь в начало обработчика сообщений продавца:
from shared.utils.chat_filter import filter_message, is_message_blocked

text = message.text or ""
if is_message_blocked(text):
    await message.answer("❌ Message contains only contact info and was blocked.")
    return
filtered_text = filter_message(text)
```

---

## Шаг 10 — Перезапустить все сервисы

```bash
docker-compose down
docker-compose up -d --build
docker-compose logs -f
```

---

## Проверочный чеклист после обновления

Выполни каждый пункт вручную:

### Приватность покупателя
- [ ] Зайди в seller_bot → Мои заказы → любой заказ → **НЕ должно быть** "Buyer ID: ..."
- [ ] Отправь в чате заказа текст `@username` → должно прийти `[КОНТАКТ СКРЫТ]`
- [ ] Отправь `+1234567890` → должно прийти `[ТЕЛЕФОН СКРЫТ]`
- [ ] Отправь `test@gmail.com` → должно прийти `[EMAIL СКРЫТ]`

### Защита от двойной покупки
- [ ] Купи товар → он должен исчезнуть из каталога (is_available = False)
- [ ] Попробуй купить тот же товар повторно → должна быть ошибка "already sold"

### Миграция БД
- [ ] `alembic current` показывает `v24_001`
- [ ] В таблице `seller_orders` есть колонки: `escrow_released`, `auto_complete_at`
- [ ] Таблица `system_settings` создана и содержит 9 записей

### Celery (если добавлен)
- [ ] `docker-compose ps` показывает `celery_worker` и `celery_beat` в статусе `Up`
- [ ] `docker-compose logs celery_beat` показывает периодические запуски

---

## Найденные проблемы v2 и их исправления

| # | Проблема в v2 | Критичность | Исправлено |
|---|---|---|---|
| P-01 | `seller_bot/handlers/orders.py:93` — продавец видит `buyer_user_id` | **КРИТИЧНО** | `orders_v24.py` |
| P-02 | `product_service.py` — нет `SELECT FOR UPDATE` → race condition | **КРИТИЧНО** | `product_service_v24.py` |
| P-03 | `product_service.py` — нет проверки `is_active` | Высокая | `product_service_v24.py` |
| P-04 | Нет идемпотентности выплат (двойная выплата возможна) | **КРИТИЧНО** | `auto_complete.py` |
| P-05 | Нет таймера автозавершения заказов | Высокая | `auto_complete.py` + `orders_v24.py` |
| P-06 | `chat_filter.py` не фильтрует email и Discord | Средняя | `chat_filter_v24.py` |
| P-07 | Нет логирования нарушений воркеров в БД | Средняя | `chat_filter_v24.py` + миграция |
| P-08 | Нет динамических констант (PLATFORM_FEE захардкожен) | Средняя | `system_settings` таблица |
| P-09 | Комиссия маркетолога начисляется сразу, не после эскроу | Высокая | `product_service_v24.py` + `auto_complete.py` |
| P-10 | Нет поля `moderation_status` у Product | Средняя | Миграция + models |

---

## Если что-то пошло не так

### Откат миграции:
```bash
alembic downgrade v24_001-1
# или полный откат:
alembic downgrade base
```

### Восстановление из бэкапа:
```bash
# PostgreSQL
psql -U newlookup -d newlookup < backup_YYYYMMDD.sql

# SQLite
cp data/newlookup_BACKUP_YYYYMMDD.db data/newlookup.db
```

### Откат кода:
```bash
cp -r /path/to/newlookup_v2_BACKUP_YYYYMMDD/* /path/to/newlookup_v2_work/
docker-compose up -d --build
```
